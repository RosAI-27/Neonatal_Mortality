# Outputs

This directory is intended for generated analytical outputs such as:

- comparison tables;
- evaluation metrics;
- figures and plots;
- model interpretation results.

The current notebook generates some outputs dynamically. Existing analysis files have deliberately not been moved or rewritten during this structural refactor.